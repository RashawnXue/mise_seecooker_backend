server:
  port: 9501
spring:
  application:
    name: seecooker-user-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
        group: DEFAULT_GROUP
  config:
    import: optional:nacos:${spring.application.name}
